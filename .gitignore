node_modules
dist
.vite
.DS_Store
*.log

# TypeScript incremental build info — regenerated by `tsc -b` / `npm run build`.
# These were tracked until 2026-07-30, which left a permanent phantom diff on
# the VM (the UI is built there) that made `git status` useless for spotting
# real drift during a laptop↔VM reconciliation.
*.tsbuildinfo
.env
.env.local
/ignored claude chats
